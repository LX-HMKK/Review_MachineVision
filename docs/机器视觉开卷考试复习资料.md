# 机器视觉开卷考试复习资料

> 适用范围：Ch02-Ch11
> 使用方式：优先看“高频考点速查”和“题型模板”，再按章节翻查细节

## 一、高频考点速查

### 1. 去年分值与今年复习优先级

| 章节 | 去年分值 | 今年复习级别 | 备注 |
|:---|:---:|:---:|:---|
| Ch03 滤波 | 22 分 | A+ 必冲 | 卷积计算、高斯/中值 |
| Ch04 边缘检测 | 16 分 | A+ 必冲 | Canny 流程、算子辨析 |
| Ch07 Blob / SIFT | 12 分 | A 高优先 | SIFT 步骤、不变性 |
| Ch09 分割 | 12 分 | A 高优先 | 形态学识图、开闭运算 |
| Ch06 角点检测 | 11 分 | A 高优先 | Harris 判据、方法对比 |
| Ch08 纹理 | 11 分 | A 高优先 | LBP、滤波器组、BoF |
| Ch10 识别 | 11 分 | A 高优先 | 生成/判别、识别流程 |
| Ch02 成像与标定 | 10 分 | A 高优先 | 内外参、投影、标定 |
| Ch05_2 霍夫变换 | 3 分 | B 要覆盖 | 参数式、投票峰值 |
| Ch11 目标检测 | 1 分 | B 要覆盖 | Viola-Jones 流程 |
| Ch05_1 拟合 | 0 分 | B- 兜底 | 最小二乘、RANSAC |

速记顺序：Ch03/04 → Ch06/07/08/09/10/02 → Ch05_2/11/05_1。

### 2. 必背公式速查

| 对象 | 章节 | 关键词 | 核心内容 | 常见题型/易错点 |
|:---|:---|:---|:---|:---|
| 相机投影模型 | Ch02 | 内参、外参、投影矩阵、坐标系 | $\mathbf{p} \sim K[R\|t]\mathbf{P}_w$ | 投影模型题；易把 `K`、`R/t` 的物理意义和所属坐标系写混 |
| 透视投影 | Ch02 | 透视效应、深度缩放、近大远小 | $x'=\dfrac{fx}{z},\ y'=\dfrac{fy}{z}$ | 解释题；易忽略深度 `z` 导致的非线性缩放 |
| 卷积输出尺寸 | Ch03 | 尺寸计算、padding、stride、边界 | $W_{out}=\left\lfloor\dfrac{W-k+2p}{s}\right\rfloor+1,\ H_{out}=\left\lfloor\dfrac{H-k+2p}{s}\right\rfloor+1$ | 计算题；易不先区分 `valid/same/full` 就直接代公式 |
| 高斯核 | Ch03 | 高斯滤波、平滑、可分离、`\sigma` | $G(x,y)=\dfrac{1}{2\pi\sigma^2}\exp\!\left(-\dfrac{x^2+y^2}{2\sigma^2}\right)$ | 概念题；易把 `\sigma` 增大与去噪增强、模糊加重的关系写反 |
| Harris 响应 | Ch06 | 角点判据、二阶矩阵、特征值 | $R=\det(M)-k(\operatorname{trace}(M))^2$ | 判别题；易把平坦区、边缘区、角点区的响应特征混淆 |
| 霍夫直线公式 | Ch05_2 | 参数空间、投票、峰值检测 | $\rho=x\cos\theta+y\sin\theta$ | 流程/辨析题；易把图像空间直线与参数空间曲线关系写反 |
| k-means 目标函数 | Ch10 | 聚类、类内误差、迭代优化 | $J=\sum_{i=1}^{k}\sum_{x\in C_i}\|x-\mu_i\|^2$ | 论述题；易把“建词典用聚类”和“最终分类器”混成同一步 |
| 开运算 / 闭运算 | Ch09 | 形态学、去噪、填孔、顺序辨析 | $A\circ B=(A\ominus B)\oplus B$；$A\bullet B=(A\oplus B)\ominus B$ | 识图题；易把开闭顺序和“去小噪点/填小孔洞”的作用对调 |

### 3. 必会流程速查

| 对象 | 章节 | 关键词 | 核心内容 | 常见题型/易错点 |
|:---|:---|:---|:---|:---|
| Canny | Ch04 | 边缘检测、NMS、双阈值、滞后连接 | 高斯平滑 → 梯度幅值/方向 → 非极大值抑制 → 双阈值 → 滞后连接 | 默写流程题；易把 NMS 与双阈值前后顺序写错 |
| SIFT | Ch07 | 尺度空间、DoG、主方向、描述子 | 高斯/DoG 金字塔 → 尺度极值 → 精定位去伪点 → 主方向 → 描述子 | 步骤题；易漏掉精定位去伪点或主方向分配 |
| Bag-of-Features | Ch08/Ch10 | 局部特征、视觉词袋、量化、分类 | 局部特征 → k-means 词典 → 特征量化 → 词频直方图 → 分类 | 论述题；易把“聚类建词典”误写成“最终分类器训练” |
| Viola-Jones | Ch11 | Haar、积分图、AdaBoost、级联分类器 | Haar 特征 → 积分图 → AdaBoost → 级联分类器 → 滑窗检测 | 流程题；易把积分图的加速对象和级联筛选顺序写错 |

### 4. 高频辨析速查

| 对象 | 章节 | 关键词 | 核心内容 | 常见题型/易错点 |
|:---|:---|:---|:---|:---|
| 高斯 vs 中值 | Ch03 | 线性滤波、非线性滤波、高斯噪声、椒盐噪声 | 高斯是线性加权平滑；中值是非线性排序滤波，分别更适合高斯噪声与椒盐噪声 | 辨析题；易把中值写成线性卷积 |
| Harris vs Moravec | Ch06 | 二阶矩阵、离散方向、稳定性、角点判据 | Harris 基于二阶矩阵与全方向信息，Moravec 只看少量离散方向，稳定性更弱 | 对比题；易忽略 Harris 的特征值判据 |
| 生成式 vs 判别式 | Ch10 | 联合分布、条件分布、决策边界、后验 | 生成式强调学习联合/条件生成过程；判别式直接学习类别边界或后验判别 | 概念题；易把 $p(x\|y)$ 和 $p(y\|x)$ 混写 |
| 分类 vs 检测 vs 分割 | Ch09/Ch10/Ch11 | 类别标签、目标定位、像素标注 | 分类输出类别，检测输出类别加位置框，分割输出像素级标签 | 综合题；易把检测框结果当成分割结果 |
| 腐蚀 / 膨胀 / 开 / 闭 | Ch09 | 形态学、缩小放大、顺序、作用 | 腐蚀使前景缩小，膨胀使前景扩大；开是先腐后胀，闭是先胀后腐 | 识图题；易把开闭顺序和“去小噪点/填小孔洞”作用对调 |

## 二、题型作答模板

### 1. 选择题 / 判断题

#### 通用作答骨架

1. 先判题干在问 `定义 / 性质 / 步骤 / 场景` 中的哪一类，再对应回忆本课术语。
2. 选择题先排除明显错项；判断题先看是否出现 `一定`、`总是`、`唯一`、`所有` 这类绝对化表述。
3. 定义题只答“它是什么”；比较题只答“核心区别”；步骤题重点看顺序；场景题重点看噪声类型、输出目标、是否要定位。

#### 最短句式模板

1. 定义题：`X 是……，其核心特征是……。`
2. 比较题：`A 与 B 的主要区别在于……；A 更强调……，B 更强调……。`
3. 判断题：`该说法错误/正确，因为……。`
4. 场景题：`若题目条件是……，更适合用……，因为……。`

#### 本课高频对象/调用规则

1. 易混概念优先成对调用：`高斯 vs 中值`、`Harris vs Moravec`、`生成式 vs 判别式`、`分类 vs 检测 vs 分割`。
2. 定义题优先回 `关键概念`；若选项涉及公式、判据或不变性，再补看对应章节的 `关键公式与符号意义`。
3. 若选项在考步骤先后，直接调用前文 `必会流程速查`，不要在这里临场展开新流程。

### 2. 计算题

#### 通用作答骨架

1. 先写已知：输入尺寸 `H×W`（或 `W×H`）、核尺寸 `k`、padding `p`、stride `s`、通道数 `C`、卷积核个数 `N`。
2. 再写输出尺寸：
   `W_out = floor((W - k + 2p) / s) + 1`
   `H_out = floor((H - k + 2p) / s) + 1`
   先判 `valid / same / full`，再代数。
3. 再写单点运算量、总运算量、可分离或积分图等优化项，最后做尺寸和量纲自检。

#### 最短句式模板

1. `已知输入为……，卷积核为……，padding 为……，stride 为……。`
2. `因此输出尺寸为……。`
3. `每个输出点需要……次乘加，所以总运算量为……。`
4. `若利用……优化，则复杂度可由……降为……。`

#### 本课高频对象/调用规则

1. 卷积题优先调用 Ch03 的输出尺寸公式、`valid/same/full` 区分和可分离核结论。
2. 若题干出现高斯核，正文最后补一句 `高斯核可分离`；若出现积分图，补一句 `矩形区域求和可常数时间完成`。
3. 公式、符号、复杂度写法不确定时，回看对应章节的 `关键公式与符号意义`，按原符号抄写，不自行换字母。

### 3. 流程题

#### 通用作答骨架

1. 本题只回答 `怎么做，按什么顺序`，不展开和别的方法谁更好。
2. 固定写法：`输入 -> 核心步骤 -> 每步作用 -> 输出`，每步只写一句 `做什么 + 为什么做`。
3. 高频对象参见前文流程速查表，正文展开时按该骨架写，不必在这里重复抄整套细节。

#### 最短句式模板

1. `输入为……。`
2. `首先……，作用是……。`
3. `然后……，作用是……。`
4. `最后……，得到……。`

#### 本课高频对象/调用规则

1. 默认优先调用：`Canny`、`SIFT`、`Bag-of-Features`、`Viola-Jones`。
2. 写流程时先对照前文 `必会流程速查` 的顺序，再把每步作用补成一句话。
3. 若题干只问某一步作用，就截取该步及前后衔接，不要把整条流程全抄上去。

### 4. 论述题

#### 通用作答骨架

1. 本题回答 `为什么这样做，与别的方法相比如何`，不是只默写步骤。
2. 固定骨架：`方法目标 -> 核心思想 -> 关键公式/判据 -> 优缺点 -> 与相近方法比较 -> 适用场景`。
3. 开头先钉住问题对象，结尾一定落到“适合什么，不适合什么”。

#### 最短句式模板

1. `该方法的目标是……。`
2. `它这样做的原因是……，核心思想是……。`
3. `关键公式/判据是……，其中……表示……。`
4. `与……相比，它的优点是……，缺点是……。`
5. `因此它更适用于……，不太适用于……。`

#### 本课高频对象/调用规则

1. 高频论述对象优先调用：`生成式 vs 判别式`、`高斯 vs 中值`、`Harris vs Moravec`、`LBP / BoF`、`分类 vs 检测 vs 分割`。
2. 若论述题涉及公式，至少点一个章节中的核心公式或符号意义；若涉及方法比较，优先从目标、信息利用方式、鲁棒性、代价四个角度对比。
3. 展开正文时，先回该章节 `关键概念` 与 `关键公式与符号意义`，再把 `可能出题方式` 中对应题型的要点改写成完整答案。

#### 与章节正文的映射规则

1. `关键概念` 主要对应选择/判断题，用来答定义、性质、辨析。
2. `关键公式与符号意义` 主要对应计算题、论述题，用来答代数步骤、判据来源、符号解释。
3. `可能出题方式` 使用时按四类题型分开看：
   选择/判断题：优先抽定义、性质、易混概念。
   计算题：优先抽公式代入、复杂度、符号说明。
   流程题：优先抽步骤顺序、每步作用、输入输出。
   论述题：优先抽方法目标、核心思想、优缺点、对比对象。

## 三、分章节完整复习资料

### Ch02 成像与标定
#### 本章核心考点
#### 关键概念
#### 关键公式与符号意义
#### 高频比较与易错点
#### 可能出题方式

### Ch03 滤波
#### 本章核心考点
#### 关键概念
#### 关键公式与符号意义
#### 高频比较与易错点
#### 可能出题方式

### Ch04 边缘检测
#### 本章核心考点
#### 关键概念
#### 关键公式与符号意义
#### 高频比较与易错点
#### 可能出题方式

### Ch05_1 拟合
#### 本章核心考点
#### 关键概念
#### 关键公式与符号意义
#### 高频比较与易错点
#### 可能出题方式

### Ch05_2 霍夫变换
#### 本章核心考点
#### 关键概念
#### 关键公式与符号意义
#### 高频比较与易错点
#### 可能出题方式

### Ch06 角点检测
#### 本章核心考点
#### 关键概念
#### 关键公式与符号意义
#### 高频比较与易错点
#### 可能出题方式

### Ch07 Blob / SIFT
#### 本章核心考点
#### 关键概念
#### 关键公式与符号意义
#### 高频比较与易错点
#### 可能出题方式

### Ch08 纹理
#### 本章核心考点
#### 关键概念
#### 关键公式与符号意义
#### 高频比较与易错点
#### 可能出题方式

### Ch09 分割
#### 本章核心考点
#### 关键概念
#### 关键公式与符号意义
#### 高频比较与易错点
#### 可能出题方式

### Ch10 识别
#### 本章核心考点
#### 关键概念
#### 关键公式与符号意义
#### 高频比较与易错点
#### 可能出题方式

### Ch11 目标检测
#### 本章核心考点
#### 关键概念
#### 关键公式与符号意义
#### 高频比较与易错点
#### 可能出题方式

## 四、最后冲刺页

后续将填入高频公式总表、方法步骤总表和考前快速翻查清单。
